# 중앙 공개 앱 · A

진입점: `main:app`. 현재 `/healthz`만 제공하며 업무 기능 준비 상태는 false다.
경계: 공개 카탈로그·판매자 접속 위치만 제공한다. 구매·주문·고객 이력·추천 요청을 중앙으로 중계하거나 저장하지 않는다.

## 환경변수

`gate docs`가 첫 줄을 이 서비스 디렉터리의 코드 전체와 대조한다. 코드에서 새 값을 읽으면 같은 PR에서 이 줄을 고친다.

- 현재 코드가 읽는 값: 없음
- 구현 시 추가: `CENTRAL_DB_PATH`
