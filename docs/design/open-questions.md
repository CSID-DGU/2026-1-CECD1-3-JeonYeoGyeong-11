# 구현 전에 확정할 항목

현행 검토에서 남은 항목만 모았다. 아래는 구현이 끝났다는 뜻도, 제안한 해결책이 이미 합의됐다는 뜻도 아니다. 담당자는 해당 구현 전에 코드/계약/검사를 함께 보완한다. 독립적인 작업은 계속할 수 있다.

| ID | 담당·시점 | 결정/검증할 내용 | 완료 증거 |
| --- | --- | --- | --- |
| Q01 | B · 본 학습 전 | 양성 선택·음성 샘플·손실 함수·배치 집계 정의 | 수작업 가능한 작은 loss/gradient 예제와 기록된 config |
| Q02 | B · 본 학습 전 | 학습 시작 snapshot 안에서도 각 예제의 과거 cutoff만 쓰기 | 미래 target/이벤트를 바꿔도 이전 예제 특징 불변 |
| Q03 | B, A · 데이터/서비스 연결 전 | 원자료→catalog→text builder의 동일한 입력 의미 | 두 출처 각각 서비스 왕복 후 텍스트·token·z 일치 |
| Q04 | B · 성능 해석 전 | 동일 텍스트·관계 0 상품의 구별 불가능성과 충돌률 | DH/IC/live 정규화·token 단계 충돌 측정, 한계 명시 |
| Q05 | B, C · FL 이득 비교 전 | local-only의 초기값·학습량·선택/개인화 예산을 FL와 맞추기 | 같은 split/후보/예산의 재현 가능한 실행 설정 |
| Q06 | B · 전처리 확정 전 | DH 주이용 점포 배정 뒤 타 점포 거래 처리·표본 선정 시점 | 유지/제외 행 수와 판매자 간 고객 중복 검사 |
| Q07 | A, B · 실제 이벤트 연결 전 | 상품보다 구매가 먼저 도착할 때 보류/재시도·비교 준비 상태 | 역순·상품 전달 실패·재시작 뒤 1회 반영 |
| Q08 | A, B, C · 실학습 연결 전 | 수동/자동 트리거·빈 데이터 client·중단/재시작·모델 교체 | 학습 중 서빙·중복 거부·실패/재시작·새 base 전환 |
| Q09 | A, B, C · 실제 추천 연결 전 | 모델 없는 fallback의 버전 ID·점수 의미 | 모델 미설치/빈 후보/무이력의 유효 응답과 화면 |
| Q10 | B, C · 실데이터 평가 보고 전 | 평가 지표도 개별 값을 중앙에 모으지 않는 집계 방법 | 허용된 집계 출력·중앙 비잔류 검사 |
| Q11 | B 비용 측정, A/C 서비스 연결 · 소형 실행 후 | 판매자별 계산 예산·한 PC 순차 실행의 deadline·학습 중 서비스 자원 경합 | 장치·데이터 규모·설정을 기록한 추천/학습 시간·최대 메모리, 학습 유무의 주문/추천 응답 비교. 절차는 model-lab.md §5 |
| Q12 | B · 어댑터 확정 전 | Dunnhumby 절대시각의 시간대(원자료에 기록이 없다)와 원자료 입력 형식(.rds 직접 읽기는 lock에 없는 의존성 필요, 또는 CSV 내보내기) | 선택과 근거를 기록한 어댑터 fixture. 시간대를 추정하면 추정이라고 표시 |
| Q13 | A, C · 인증 API 추가 전 | 인증 실패(401)의 `contract_error.v1` 코드. 현재 v1 enum에 인증 오류 코드가 없고 strict v1이라 추가도 계약 변경이다 | 결정 기록과 schema·fixture·오류 매핑 |

A08은 초기 뼈대에서 단일 worker/runtime·공유 실행기·중복/실패 거부·health 응답까지 부분 반영했다. 실학습·모델 교체·재시작 복구까지 해결한 것은 아니다.

## 이미 열린 구현 선택

- B: NLP model ID/revision·라이선스·tokenizer·길이/차원, 관계 이웃/시간쌍 예산, 개인화 step/lr/최소 데이터/validation 기준.
- C: 보호 프로토콜/버전·위협 가정·참여/이탈 하한·양자화·메시지 schema·실패/재시도. 보호 모드 최소 5명과 선택 프로토콜 조건을 함께 충족해야 한다.
- B/C: 화면 없는 실험에서 재사용할 집계 core API와 초기 release 등록 절차.
- 팀: 역할별 계정 선점(각 카드 2행), 제출일·발표 요구. CODEOWNERS 승인을 GitHub에 강제할지: 필수 승인 수를 1로 올리면 모든 PR이 승인을 기다리고, 0으로 두면 에이전트가 확인한다([Git 협업](../team/git-workflow.md) 'CI와 저장소 설정').

판매자가 통제하는 서버 환경의 존재를 시나리오 전제로 두고, 실제 서버 조달·설치·운영 제공 방식은 이번 범위 밖으로 둔다. 한 PC에서 논리적 판매자들을 모사하는 것이 기본이며 여러 PC 배치는 선택적 시연이다. 이는 서버 배포나 보안 구현을 완료했다는 뜻이 아니다([아키텍처](architecture.md)).

작은 고정 표본부터 비용과 작동 여부를 확인한다. test 결과로 hyperparameter를 고르지 않고, 합성 통과를 원자료 재현·모델 성능·보호 검증으로 대체하지 않는다. 미구현 게이트는 미구현 상태를 유지한다.
