# Composable Graph Algorithm Interfaces

이 문서는 `docs/framework/components.md`로 통합되었다.

핵심 내용:

- lifecycle layer와 interface map을 관리한다.
- `graph_source`, `graph_builder`, `aggregation_target`, artifact contract를 관리한다.
- support level과 extension workflow를 canonical 문서에서 관리한다.

Canonical:

- `docs/framework/components.md`
