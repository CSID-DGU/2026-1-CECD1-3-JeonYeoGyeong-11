# Diagnostic Metric Guide

이 문서는 `docs/framework/metrics.md`로 통합되었다.

핵심 내용:

- `DI`, `N_eff`, alignment, `LOO` 정의를 관리한다.
- `round_metrics.csv`, `client_metrics.csv`, `graph_stats.csv` 해석 기준을 관리한다.
- synthetic metric validity는 evidence 문서와 연결된다.

Canonical:

- `docs/framework/metrics.md`
- `docs/framework/evidence.md`
