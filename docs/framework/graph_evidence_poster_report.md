# Graph-FL Framework Evidence Pack Report

이 문서는 `docs/framework/evidence.md`로 통합되었다.

핵심 내용:

- poster claim과 evidence summary를 관리한다.
- `graph_parity_summary.csv`, `external_mechanism_alignment.csv`, `metric_validity_summary.csv`, `design_space_matrix.csv`, `extension_contract_summary.csv`를 연결한다.
- figure와 poster table은 evidence artifact에서 재생성한다.

Canonical:

- `docs/framework/evidence.md`
