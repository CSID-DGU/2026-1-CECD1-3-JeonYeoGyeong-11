# Prior Work Mapping

이 문서는 `docs/framework/components.md`로 통합되었다.

핵심 내용:

- FedAMP, SFL, pFedGraph, FedAGA mapping을 관리한다.
- support vocabulary는 `core-supported`, `proxy-supported`, `paper-kernel`, `exact-reference`, `interface-target`로 구분한다.
- pFedGraph component boundary는 evidence 문서와 연결된다.

Canonical:

- `docs/framework/components.md`
- `docs/framework/evidence.md`
