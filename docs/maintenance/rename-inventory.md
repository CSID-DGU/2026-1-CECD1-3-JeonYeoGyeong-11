# Rename Inventory

이 문서는 `docs/maintenance/migration-and-compatibility.md`의 source stub다.

핵심 내용:

- `graphfl_lab`, `run_vision_*`, `vision_suite_*`, `result_vision_*`가 canonical surface다.
- `configs/general/...`, `spectral_filter_strength`, `spectral_filtered_*`는 compatibility alias다.
- `spectral_fl`, `run_general_*`, `general_suite_*`, `result_general_*`는 removed surface다.

Canonical:

- `docs/maintenance/migration-and-compatibility.md`
